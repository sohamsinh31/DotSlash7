import { Button } from "../Button/Button"
import "./Navbar.css"

export const Navbar = () => {
    return (
        <div className="navbar">
            <Button>Back</Button>
            <Button>Back</Button>
        </div>
    )
}